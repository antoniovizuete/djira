import { UpdateFieldConfigurationSchemeDetails } from '../models/index.ts';

export interface UpdateFieldConfigurationScheme extends UpdateFieldConfigurationSchemeDetails {
  /** The ID of the field configuration scheme. */
  id: number;
}
