import { CustomFieldContextDefaultValue } from './customFieldContextDefaultValue.ts';

/** Default values to update. */
export interface CustomFieldContextDefaultValueUpdate {
  defaultValues?: CustomFieldContextDefaultValue[];
}
